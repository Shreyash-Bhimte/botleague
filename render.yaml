services:
  - type: web
    name: botleague-server
    runtime: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: MONGO_URI
        sync: false
    pythonVersion: "3.11.9"